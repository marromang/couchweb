### INSTALACIÓN DE ICINGA SOBRE UBUNTU 14.04 ###
https://www.digitalocean.com/community/tutorials/how-to-monitor-hosts-and-services-with-icinga-on-ubuntu-16-04

# Servidor LAMP

# Instalacion
## Descargamos la clave de icinga
curl -sSL https://packages.icinga.com/icinga.key | sudo apt-key add -

## Añadimos sources.list
sudo nano /etc/apt/sources.list.d/icinga.list
deb https://packages.icinga.com/ubuntu icinga-xenial main
apt update

    Enable Icinga2 ido-mysql feature? YES
    Configure database for icinga2-ido-mysql with dbconfig-common? YES
    You'll then be prompted to set up an Icinga database password. Create a strong password and record it for later. We'll need it when setting up the web interface.


## enable the Icinga database backend
sudo icinga2 feature enable ido-mysql command
sudo systemctl restart icinga2

# Interfaz web
sudo apt-get install icingaweb2

## Hora php
sudo nano /etc/php/7.0/apache2/php.ini
date.timezone=Europe/Madrid

sudo systemctl restart apache2
## setup token
sudo icingacli setup token create
## acceso
https://jarvis.maria.org/icingaweb2

## User Database Setup
    Resource Name: icingaweb_db
    Database Type: MySQL
    Host: localhost
    Port:
    Database Name: icingaweb_users
    Username: icingaweb
    Password: set and record a password
    Character Set:
    Persistent: leave unchecked
    Use SSL: leave unchecked

## Set up Monitoring Database
    Resource Name: icinga_ido
    Database Type: MySQL
    Host: localhost
    Port:
    Database Name: icinga2
    Username: icinga2
    Password: password you created during installation
    Character Set
    Persistent: unchecked
    Use SSL: unchecked

## Alertas
sudo apt-get install ssmtp mailutils
sudo nano /etc/ssmtp/ssmtp.conf
	mailhub=mail.example.com:465
	UseTLS=yes
	FromLineOverride=yes
	AuthUser=smtp_username
	AuthPass=smtp_password
echo "hello world" | mail -s "test subject" sammy@example.com

sudo nano /etc/icinga2/conf.d/users.conf
email = "sammy@example.com"
sudo systemctl restart icinga2

### PRUEBA:
stress --cpu 6

# Integración de nuevos clientes
